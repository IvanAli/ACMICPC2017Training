#include <bits/stdc++.h>
#define snd second
#define fst first

using namespace std;

int n;
int L,R;
pair <int,int> st[200005];
vector <int> arr[200005];
vector <int> pat;

int main()
{
	ios_base::sync_with_stdio(0); cin.tie(0);
	cin>>n>>L>>R;
	int x,y;
	for(int i = 0; i < n; ++i)
	{
		cin>>x>>y;
		st[x].insert({-i,y});
	}

	pat.pb(0);
	int curr = 0;
	while(curr != -1)
	{
		if(st[curr].size() != 0)
		{
			pair <int,int> tmp = st[curr].begin();
			st[curr].erase(st[curr].begin());
			arr[curr]
		}
		else
		{
			if(curr == 0)
				curr = -1;
			else
				curr = 0;
		}
	}


	return 0;

}


int n;
int L,R;
pair <int,int> st[200005];
vector <int> arr[200005];
vector <int> pat;

int main()
{
	ios_base::sync_with_stdio(0); cin.tie(0);
	cin>>n>>L>>R;
	int x,y;
	for(int i = 0; i < n; ++i)
	{
		cin>>x>>y;
		st[x].insert({-i,y});
	}

	pat.pb(0);
	int curr = 0;
	while(curr != -1)
	{
		if(st[curr].size() != 0)
		{
			pair <int,int> tmp = st[curr].begin();
			st[curr].erase(st[curr].begin());
			arr[curr]
		}
		else
		{
			if(curr == 0)
				curr = -1;
			else
				curr = 0;
		}
	
int n;
int L,R;
pair <int,int> st[200005];
vector <int> arr[200005];
vector <int> pat;

int main()
{
	ios_base::sync_with_stdio(0); cin.tie(0);
	cin>>n>>L>>R;
	int x,y;
	for(int i = 0; i < n; ++i)
	{
		cin>>x>>y;
		st[x].insert({-i,y});
	}

	pat.pb(0);
	int curr = 0;
	while(curr != -1)
	{
		if(st[curr].size() != 0)
		{
			pair <int,int> tmp = st[curr].begin();
			st[curr].erase(st[curr].begin());
			arr[curr]
		}
		else
		{
			if(curr == 0)
				curr = -1;
			else
				curr = 0;
		}
	
int n;
int L,R;
pair <int,int> st[200005];
vector <int> arr[200005];
vector <int> pat;

int main()
{
	ios_base::sync_with_stdio(0); cin.tie(0);
	cin>>n>>L>>R;
	int x,y;
	for(int i = 0; i < n; ++i)
	{
		cin>>x>>y;
		st[x].insert({-i,y});
	}

	pat.pb(0);
	int curr = 0;
	while(curr != -1)
	{
		if(st[curr].size() != 0)
		{
			pair <int,int> tmp = st[curr].begin();
			st[curr].erase(st[curr].begin());
			arr[curr]
		}
		else
		{
			if(curr == 0)
				curr = -1;
			else
				curr = 0;
		}
	
int n;
int L,R;
pair <int,int> st[200005];
vector <int> arr[200005];
vector <int> pat;

int main()
{
	ios_base::sync_with_stdio(0); cin.tie(0);
	cin>>n>>L>>R;
	int x,y;
	for(int i = 0; i < n; ++i)
	{
		cin>>x>>y;
		st[x].insert({-i,y});
	}

	pat.pb(0);
	int curr = 0;
	while(curr != -1)
	{
		if(st[curr].size() != 0)
		{
			pair <int,int> tmp = st[curr].begin();
			st[curr].erase(st[curr].begin());
			arr[curr]
		}
		else
		{
			if(curr == 0)
				curr = -1;
			else
				curr = 0;
		}
	
int n;
int L,R;
pair <int,int> st[200005];
vector <int> arr[200005];
vector <int> pat;

int main()
{
	ios_base::sync_with_stdio(0); cin.tie(0);
	cin>>n>>L>>R;
	int x,y;
	for(int i = 0; i < n; ++i)
	{
		cin>>x>>y;
		st[x].insert({-i,y});
	}

	pat.pb(0);
	int curr = 0;
	while(curr != -1)
	{
		if(st[curr].size() != 0)
		{
			pair <int,int> tmp = st[curr].begin();
			st[curr].erase(st[curr].begin());
			arr[curr]
		}
		else
		{
			if(curr == 0)
				curr = -1;
			else
				curr = 0;
		}
	
